# iOS ショートカット（入力側）の作り方

iPhone の背面タップで、iCloud Drive 上の `log.txt` に打刻を1行追記する仕組み。
画面を触らずに完結する。PWA（閲覧側）は [docs/](docs/) を参照。

> ここに書いたアクション名は iOS 17 の日本語 UI での表記。iOS のバージョンで
> 細部が違うことがある。実機で試して違っていたら README の「次にやること」に書き足す。

## 0. 準備（ファイル置き場）

1. 「ファイル」アプリ → iCloud Drive → **Shortcuts** フォルダを開く
   （ショートカットのファイル系アクションが追加設定なしで読み書きできる既定の場所）。
2. その中に **retainer-log** フォルダを作る。
3. （任意）PC にある既存の `log.txt`（1月分の手書き形式）をこのフォルダにコピーする。
   打刻行は同じファイルの末尾に足されていくが、形式は混在してよい（集計側が両方読む）。
   コピーしなければ、最初の打刻時にファイルが新規作成される。

置き場所: `iCloud Drive/Shortcuts/retainer-log/log.txt`

## 1. ショートカット「リテーナー打刻」（背面タップ用）

1行の形式は次のどちらか。**「外」= 外した、「着」= つけた**。

```
2026/09/13 08:24 外
2026/09/13 09:22 着
```

どちらを書くかは、ファイルの最終行を見て自動で決める（最終行が「外」なら今回は「着」、それ以外なら「外」）。
つまりタップするたびに 外 → 着 → 外 → … と交互に記録される。

### アクション列

| # | アクション | 設定 |
|---|-----------|------|
| 1 | **日付** | 「現在の日付」 |
| 2 | **日付をフォーマット** | 入力: 日付 ／ 日付フォーマット: **カスタム** ／ フォーマット文字列: `yyyy/MM/dd HH:mm` |
| 3 | **変数を設定** | 名前: `時刻` ／ 入力: フォーマット済みの日付 |
| 4 | **ファイルを取得** | サービス: Shortcuts（iCloud Drive） ／ ファイルパス: `retainer-log/log.txt` ／ 「見つからない場合はエラー」**オフ** |
| 5 | **テキストを分割** | 入力: ファイル ／ 区切り: **改行** |
| 6 | **リストから項目を取得** | **最後の項目** |
| 7 | **変数を設定** | 名前: `最終行` ／ 入力: 項目 |
| 8 | **if** | `最終行` が **次を含む** `外` |
| 9 | 　**変数を設定** | 名前: `種別` ／ 入力: テキスト `着` |
| 10 | 　**変数を設定** | 名前: `表示` ／ 入力: テキスト `つけた` |
| 11 | **その他の場合** | |
| 12 | 　**変数を設定** | 名前: `種別` ／ 入力: テキスト `外` |
| 13 | 　**変数を設定** | 名前: `表示` ／ 入力: テキスト `外した` |
| 14 | **if の終了** | |
| 15 | **テキスト** | `時刻 種別`（変数を半角スペースでつなぐ。例 `2026/09/13 08:24 外`） |
| 16 | **テキストファイルに追加** | テキスト: 上のテキスト ／ サービス: Shortcuts ／ ファイルパス: `retainer-log/log.txt` ／ 「改行を追加」**オン** |
| 17 | **通知を表示** | タイトル: `リテーナー` ／ 本文: `表示 時刻`（例 `外した 2026/09/13 08:24`） ／ 「サウンドを再生」お好みで |

補足:

- 手順 9〜13 で「テキスト」アクションを挟まず、「変数を設定」の入力欄に直接文字を打ってよい。
- 手順 4 で `log.txt` が無いとき、5〜7 は空になり、8 は「含まない」側に進んで `外` になる。初回はこれで正しい。
- 「テキストファイルに追加」はファイルが無ければ作る。フォルダは事前に作っておく（0 章）。
- 通知に「外していた時間」も出したいなら、9 の直後に
  **入力から日付を取得**（入力: `最終行`）→ **日付の間の時間を取得**（単位: 分、その日付から 現在の日付）→ その結果を 17 の本文に足す。
  最終行 `2026/09/13 08:24 外` の中の日時を Shortcuts が拾ってくれる。
- 「デバイスを振動」アクション（iOS 17 以降の iPhone）を 17 の前に入れると、音を鳴らさなくても手応えで分かる。

### ショートカット自体の設定（右上 (i) または「詳細」）

- 「実行時に表示」オフ（背面タップからは画面を出さない）
- 「共有シートに表示」オフ

### 背面タップに割り当てる

設定 → アクセシビリティ → タッチ → 背面タップ → **ダブルタップ** → 「リテーナー打刻」

トリプルタップは空けておくか、誤作動が多いならこちらにする（トリプルの方が誤作動しにくい）。

## 2. ショートカット「リテーナー取り消し」（最後の1行を消す）

背面タップの誤作動や、外した/つけたの取り違えを直すため。ショートカットアプリ・ウィジェット・Siri（「リテーナー取り消し」）から実行する。

| # | アクション | 設定 |
|---|-----------|------|
| 1 | **ファイルを取得** | Shortcuts ／ `retainer-log/log.txt` |
| 2 | **テキストを分割** | 区切り: 改行 |
| 3 | **変数を設定** | 名前: `行` ／ 入力: 分割されたテキスト |
| 4 | **リストから項目を取得** | 最後の項目（消す行。通知用） → 変数 `消す行` |
| 5 | **カウント** | 項目 ／ 入力: `行` |
| 6 | **計算** | カウント **−** 1 |
| 7 | **リストから項目を取得** | 入力: `行` ／ **範囲内の項目** ／ 1 から 計算結果 まで |
| 8 | **テキストを結合** | 区切り: 改行 |
| 9 | **ファイルを保存** | サービス: Shortcuts ／ 「保存先を尋ねる」**オフ** ／ 保存先パス: `retainer-log/log.txt` ／ 「既存のファイルを上書き」**オン** |
| 10 | **通知を表示** | 本文: `取り消しました: 消す行` |

**もっと簡単な直し方**: 間違えて打刻したと気づいたら、**すぐもう一度背面タップ**する。
同じ分に 外→着 が並ぶと「0分のペア」になり、集計では無視される（＝実質取り消し）。
1分以上たってしまったときはこのショートカットで消す。

手動で直すときは、ファイルアプリでは .txt を編集できないので、テキスト編集アプリ
（Textastic, Koder など）で `log.txt` を開いて行を消す・足す。行の並び順は問わない
（集計側が時刻順に並べ替える）ので、忘れた「着」を末尾に足しても正しく組になる。

## 3. （任意）ショートカット「リテーナー記録を見る」

PWA をホーム画面から開いたときはボタンで `log.txt` を選ぶ必要がある（2タップ）。
それすら省きたいときは、このショートカットで **ファイルの中身を URL に載せて Safari で開く**。
データは URL の `#` 以降（フラグメント）に入るので、サーバーには送られない。

| # | アクション | 設定 |
|---|-----------|------|
| 1 | **ファイルを取得** | Shortcuts ／ `retainer-log/log.txt` |
| 2 | **Base64 エンコード** | モード: エンコード ／ 改行: **なし** |
| 3 | **URL** | `https://felesrufa.github.io/retainer-log/#d=` の直後に「Base64 エンコード済み」の変数を置く |
| 4 | **URL を開く** | |

注意: ホーム画面のアプリ（PWA）ではなく Safari のタブで開く（iOS はショートカットからの URL を PWA に渡さない）。
Safari で開いても内容は同じで、読み込んだデータは Safari 側にも保存される。
1年分（数十 KB）を超えるあたりで URL が長すぎて開けなくなる可能性がある。そうなったらボタン経由に戻す。

## 4. PWA（閲覧側）をホーム画面に置く

1. GitHub のリポジトリ設定 → Pages → Build and deployment → Source: **Deploy from a branch** → Branch: **main**、フォルダ: **/docs** → Save。
   数分後に `https://felesrufa.github.io/retainer-log/` で開けるようになる。
2. iPhone の Safari でその URL を開く → 共有ボタン → **ホーム画面に追加**。
3. ホーム画面のアイコンから開き、「log.txt を読み込む」→ ファイル一覧で
   iCloud Drive › Shortcuts › retainer-log › **log.txt** を選ぶ。
4. 以降は開くだけで前回読み込んだ内容が出る。最新にしたいときはまたボタンを押す
   （「最近使った項目」に log.txt が出るので2タップ）。

PWA はデータをどこにも送らない。読み込んだテキストは端末の localStorage に保存されるだけ。
