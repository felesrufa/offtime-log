# 設計メモ

iPhone で、背面タップ → `log.txt` 打刻 → PWA で閲覧、を成り立たせるための設計と、
聞かずに仮決めしたことの理由。あとで変えたくなったときに「なぜこうしたか」を思い出すためのメモ。

## 全体像

```
 iPhone                                                   PC
 ┌──────────────────────┐                                 ┌──────────────────┐
 │ 背面タップ            │                                 │ python aggregate.py
 │   └─ ショートカット   │  追記                           │   （画面表示 / --json）
 │      「オフ打刻」      ├────► iCloud Drive/Shortcuts/    │        ▲
 │                      │      offtime-log/log.txt ◄──── 同期 ── ┘ (必要なら手動コピー)
 │ ホーム画面の PWA      │  ファイルを選んで読む(2タップ)
 │  「オフ記録」         ◄────┘   or  #d=<base64> で URL から
 │   docs/index.html    │
 │   docs/aggregate.js  │ ← aggregate.py と同じ集計ロジック（tests/parity.py で一致確認）
 └──────────────────────┘
        ▲ 配信: GitHub Pages (main ブランチ /docs)。データは載らない。
```

- **入力**: ショートカットが `log.txt` の末尾に `2026/03/10 07:05 START` のような行を1行追記する。
- **表示**: PWA は閲覧専用。`log.txt` を読んで集計を出す。ロジックは `aggregate.py` を JS に移植したもの。
- **PC**: 従来どおり `python aggregate.py` でも同じファイルを集計できる（打刻形式に対応させた）。

## 仮決めしたことと理由

### 1. ログ形式は「手書き形式」と「打刻形式」の混在を許す（並び順も問わない）

- ショートカットの「テキストファイルに追加」は末尾追記しかできない。既存の `log.txt` は新しい日付が上の降順なので、
  同じファイルに足すと順序が混ざる。→ 集計側で **日付順に並べ替えてから** 判定する方式に変えた。
- 既存の1月分（手書き）をそのまま活かせる。手書き形式で追記・修正してもよい。
- 副作用: 「日付の飛び」判定はファイル順ではなくソート後の隣接日で見るようになった（結果は同じ）。
  同じ日付が2回ある場合は `[重複日付]` を出す。手書きと打刻が同じ日にあれば合算して `[重複日付]` を出す。

### 2. 打刻行には START / STOP のマーカーを付ける

- ユーザー要望は「時刻を1行追記」だったが、時刻だけ（奇数行=外した、偶数行=つけた）にすると、
  1回の打ち漏れ・誤作動でそれ以降が全部ずれ、しかも集計では検出できない。
- マーカーがあれば「START が2回続く」「STOP の前に START がない」を `[打刻不整合]` として検出でき、
  直すべき行が特定できる。
- マーカーの決定はショートカット側で行う（最終行が START なら今回は STOP、それ以外なら START）。
  ユーザーは1つのジェスチャを繰り返すだけ。

### 3. 背面タップは1ジェスチャのトグル方式（ダブル=START/トリプル=STOP の固定方式にしなかった）

- 要望（「背面タップから追記」）に一番近く、背面タップの枠を1つしか使わない。
- 欠点: 誤作動で状態が反転すると、次の本当のタップから逆に記録される。対策として
  - 打刻のたびに通知で「開始 / 終了」を出す（見れば気付く）。
  - **同じ分に START→STOP が並んだら 0 分ペアとして無視**する。誤作動に気付いたらすぐもう一度タップすれば取り消しになる。
  - 「オフ取り消し」ショートカット（最後の1行を消す）を用意した。
- 固定方式にしたくなったら、ショートカットを2つ作って `種別` を `START` / `STOP` に固定するだけでよい（集計側は変更不要）。

### 4. 区分（朝・昼・夜）は外した時刻で自動判定。境界は 11:00 と 16:00

- 打刻には区分がないので、集計側で決めるしかない。
- 判定ルール: 外した時刻が 11:00 より前なら【朝】、16:00 より前なら【昼】、それ以降は【夜】。
  既存の手書きデータをこのルールで分類し直しても、元の区分と1件も食い違わないことを確認して決めた。
- 変えたいときは `aggregate.py` の `NOON_START_MIN` / `EVENING_START_MIN` と、
  `docs/aggregate.js` の同名定数の **両方** を直す。

### 5. PWA は iCloud Drive を直接読めないので「ファイルを選ぶ」方式 + 端末内キャッシュ

- iOS Safari の PWA には、iCloud Drive 上のファイルを勝手に読む API がない
  （File System Access API 非対応。`<input type="file">` で毎回選んでもらうしかない）。
- 読んだテキストは `localStorage` に保存し、次回起動時はまずそれを表示する。
  最新にしたいときだけボタン → ファイル一覧の「最近使った項目」から `log.txt` を選ぶ（2タップ）。
- ボタンすら省きたい人向けに、`#d=<base64>` を付けた URL で開くとそれを読み込む経路も用意した
  （ショートカットからファイル内容を URL に載せて Safari で開く。SHORTCUTS.md の 3 章）。
  データは URL フラグメントなのでサーバーに送られない。ただし Safari のタブで開く（PWA 本体には渡らない）。

### 6. 配信は GitHub Pages（このリポジトリの `docs/`）

- ホーム画面に置ける PWA には https の URL が要る。すでに公開リポジトリがあるのでそのまま使う。
- `docs/` にはアプリ本体だけを置き、`log.txt` は今までどおり `.gitignore` で除外（データは公開しない）。
- Pages の有効化はリポジトリ設定で行う（SHORTCUTS.md の 4 章）。ここは手作業。

### 7. その他の細かい仮決め

| 事項 | 決めたこと | 理由 |
|------|-----------|------|
| ファイルの置き場 | `iCloud Drive/Shortcuts/offtime-log/log.txt` | Shortcuts フォルダ配下なら追加の許可なしで読み書きできる |
| 打刻の時刻形式 | `yyyy/MM/dd HH:mm`（ゼロ埋め） | 手書き形式の `2026/3/10`と同じ区切り。パーサは1〜2桁どちらも受ける |
| 日付をまたぐ START→STOP | 24:00 で2つに分割し `[日跨ぎ]` を出す | 夜間は装着している前提なので稀。分割しておけば日合計が壊れない |
| 2日以上離れた START→STOP | 無視して `[打刻不整合]` | ほぼ確実に打ち忘れ。合算すると4時間超過の嵐になる |
| 今日の START に STOP がない | 警告にせず「外し中」として表示 | 使用中は当然その状態になる。PWA は経過分を今日の合計に足して見せる（Python は足さない） |
| 今日の区分欠け | 警告しない | 夜になるまで【夜】は無いのが正常 |
| 同じ日に同じ区分行が2回（手書き） | 追記して合算（以前は後の行で上書き） | 上書きは黙ってデータを捨てるので危険 |
| ファイル先頭の BOM | 無視する（Python は `utf-8-sig`） | iOS のエディタや Windows のメモ帳が付けることがある |
| 平均の丸め | JS 側も Python の `round()`（偶数丸め）に合わせた | 表示を一致させるため |
| PWA のオフライン | Service Worker でアプリ本体だけキャッシュ | 電波がなくても開ける。更新は `sw.js` の `CACHE_VERSION` を上げる |

### 8. リポジトリ名・アプリ名は仮に「offtime-log」「オフ記録」

- 公開にあたり、用途を直接示す語を追跡ファイルから外した。新しい名前は指示に
  入っていなかったので、「外していた時間 = off time」から **offtime-log** を仮決めした。アプリ名と
  ショートカット名は **オフ記録 / オフ打刻 / オフ取り消し / オフ記録を見る**。
- 別の名前にするときは、追跡ファイル全体で `offtime-log` → 新名、`OfftimeLog`（JS のグローバル名）→ 新名、
  `オフ` で始まる名前 → 新名 に置換し、GitHub のリポジトリ名・Pages の URL・iCloud のフォルダ名も合わせる。

## Python と JS の一致

- 仕様の正本は `aggregate.py`。`--json` を付けると集計結果を JSON で出す。
- `docs/aggregate.js` は同じ JSON を返すように移植した。
- `python tests/parity.py` が `tests/*.txt` と `log.txt` / `test_log.txt` について両者の JSON を比較する（node が必要）。
  集計ロジックを触ったら必ず両方直してこれを回す。

## 実機でまだ確認していないこと

- ショートカットの「テキストファイルに追加」がフォルダごと新規作成するか（→ フォルダは事前に作る手順にした）。
- 「Base64 エンコード」の改行オプションの名称と、URL に載せたときの長さ上限。
- 「デバイスを振動」アクションの有無（iOS 17 以降の想定）。
- 背面タップからの「通知を表示」がバナーとして出るか（出るはず）。
- iOS Safari の `<input type="file" accept=".txt">` がファイル一覧を直接開くか（画像を含まない accept なら直接開く挙動）。
